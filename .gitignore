# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Admin page not used while distribution
pages/admin.py 

# Virtual environments
.venv

# UserData
.env

# In development
in_dev/
/docs/superpowers

# Local dev spikes (not part of the shipped project)
spikes/

# Local release helper (personal workflow, not shipped)
release.ps1

# Personal notes
dev/

# Dev Files
BLOCKED.md
PROGRESS.md

# Local Test
test_papers/

# Sites
site/

# Agent worktrees / session state — .claude/ carries whole .venv copies (515 MB)
.claude/

# Spike output. flet's exclude matches top-level NAMES, not globs, so a stray
# root PDF also ships inside the built app — keep them out of the repo root.
/*.pdf
